<?php
use Illuminate\Support\Facades\Session;

function getCorrectDatabase()
{
    // return session('basedados');
    // return \session('basedados');
    // return Session::get('basedados');
    // $app->session('basedados');
    //session_start();

    /*if (!session_status()) {
        session_name('laravel_session');
    }*/
    //dd($_SESSION);
    //return isset($_SESSION['basedados']) ?? null;
    // return session('basedados');
    // return Session::get('basedados');
    // return $_SESSION['basedados'];

}